import { useDevice } from './useDevice';
import { Device } from './useDevice.constants';
import type { TDevice, TUseDevice } from './useDevice.type';

export { useDevice, Device, type TDevice, type TUseDevice };
